Task #15: Category-grouped product display

## What was done

### Products page (ProductsPageClient.tsx)
- Added a third view mode "category" (alongside existing Grid and List)
- Added `Tag` icon from lucide-react for the category toggle button
- The view toggle now has 3 buttons: Grid (Grid3X3) | List | Category (Tag) with border-x separator
- Added `grouped` useMemo that groups `filtered` products by category alphabetically:
  - Named categories sorted alphabetically
  - Products with no category fall into "অন্যান্য পণ্য" group at the end
- Category view renders each group as a section header + horizontal scroll row:
  - Header: "Category Name" + badge "Nটি পণ্য" + horizontal rule divider
  - Products displayed in horizontally scrollable row (w-44 cards) for mobile-friendly discovery
  - Works correctly with category pill filter, search, price filters, and sort

### Store home page (StoreHomeClient.tsx)
- Added `distinctCategoriesInFiltered` useMemo with alphabetical sort (consistent with products page)
- Added `showGrouped` boolean: true when no category pill selected AND >2 distinct categories in filtered list
- Added `groupedProducts` useMemo (named cats alphabetically + "অন্যান্য পণ্য" fallback at end)
- When `showGrouped` true: renders category headings with count badge + divider; shows `gridCols * 2` products per group (intentional — home page is a discovery surface, /products page shows all)
- When `showGrouped` false: existing flat grid + "আরো দেখুন" button unchanged

### Review comments addressed
- Category ordering now consistent (alphabetical) between home and products grouped views

## No DB/API changes needed — grouping is done client-side from existing data
## TypeScript: 0 errors
